export enum AttendanceStatus {
  PRESENT = 'present',
  ABSENT = 'absent',
}

export enum UserGender {
  FEMALE = 'female',
  MALE = 'male',
}

export enum UserType {
  PARENT = 'parent',
  TEACHER = 'teacher',
  CHILD = 'child',
}
